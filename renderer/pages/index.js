import NextHead from "next/head";
import Layout from "../comps/Layout";

export default function Main() {
  return (
    <Layout>
      <NextHead>
        <title>Main</title>
      </NextHead>
    </Layout>
  );
}
